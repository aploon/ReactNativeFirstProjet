{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".", // Assurez-vous que le chemin de base est correctement configuré
    "paths": {
      "react-native": ["node_modules/react-native-web"]
    }
  },
  "exclude": [
    "**/Pods/**",
    "node_modules"
  ]
}
